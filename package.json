{
  "name": "myanmar-dictionary-extension",
  "version": "1.0.0",
  "description": "Myanmar Dictionary Extension for Chrome and Firefox",
  "scripts": {
    "clean": "rm -rf dist && mkdir -p dist/chrome-extension dist/firefox-extension dist/tmp",
    "copy:base": "cp -r extension/src/* dist/tmp",
    "build:chrome": "npm run clean && npm run copy:base && cp extension/manifests/manifest.chrome.json dist/tmp/manifest.json && mv dist/tmp dist/chrome-extension",
    "build:firefox": "npm run clean && npm run copy:base && cp extension/manifests/manifest.firefox.json dist/tmp/manifest.json && mv dist/tmp dist/firefox-extension",
    "build:all": "npm run build:chrome && npm run build:firefox",
    "dev": "npm run build:all && echo '✅ Extensions built successfully'"
  },
  "dependencies": {
    "rabbit-node": "^1.0.4"
  },
  "author": "Thu Rein Phyo",
  "license": "MIT"
}
